test(Test fun1, ()=> {
            const false ? { fun1 } : fun1 = require('../class.js')
            const ret = fun1()
            // expect(ret)
            //    .tobe(test return)
        })
test(Test fun2, ()=> {
            const false ? { fun2 } : fun2 = require('../class.js')
            const ret = fun2()
            // expect(ret)
            //    .tobe(test return)
        })